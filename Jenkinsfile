pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "Building the project"'
                // Añadir los comandos para construir tu proyecto, por ejemplo:
                // sh 'npm install'
                // sh 'npm run build'
            }
        }
        stage('Test') {
            steps {
                sh 'echo "Running tests"'
                // Añadir los comandos para ejecutar tus pruebas, por ejemplo:
                // sh 'npm test'
            }
        }
    }
}